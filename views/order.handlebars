{{!-- <title>SIM-ESTER Order Processing</title>  likely not needed? --}}
<div class="order-checkout">
    {{!-- Check if the cart is empty --}}
    {{#if emptyCart}}
        <h1>Your shopping cart is empty!</h1>

    {{!-- Show the order summary --}}
    {{else}}
        <div class="order-summary">
            <h1>Your Order Summary</h1>
            <h2>Order Reference: {{order.id}}</h2>
            <p><strong>Customer Name:</strong> {{customer.firstName}} {{customer.lastName}}</p>
            <p><strong>Customer ID:</strong> {{customer.id}}</p>

            <h3>Products in this order:</h3>

            <table>
                <tr>
                    <th>Product Id</th>
                    <th>Product Name</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    <th>Subtotal</th>
                </tr>
                {{#each items}}
                <tr>
                    <td>{{this.id}}</td>
                    <td>{{this.name}}</td>
                    <td align="center">{{this.quantity}}</td>
                    <td align="right">${{this.price}}</td>
                    <td align="right">${{this.subtotal}}</td>
                </tr>
                {{/each}}
                <tr>
                    <td colspan="4" align="right"><b>Order Total</b></td>
                    <td align="right">${{order.total}}</td>
                </tr>
            </table>
            <h2>Order completed. Will be available for download soon...</h2>
        </div>
    {{/if}}
</div>
