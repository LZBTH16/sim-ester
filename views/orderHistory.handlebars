<div class="listorder">
    <div class="listorder-title">
        <h1>Your Order History</h1>
    </div>

    <div class="order-container">
        {{#each orders}}
            <div class="order-details">
                <h2>Order ID: {{this.orderId}}</h2>
                <p><strong>Order Date:</strong> {{this.orderDate}}</p>
                <p><strong>Total Amount:</strong> ${{this.totalAmount}}</p>

                <table>
                    <thead>
                        <tr>
                            <th>Product Name</th>
                            <th>Quantity</th>
                            <th>Price</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each this.products}}
                            <tr>
                                <td>{{this.productName}}</td>
                                <td>{{this.quantity}}</td>
                                <td>{{this.price}}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        {{/each}}

        {{#unless orders}}
            <p>No orders found.</p>
        {{/unless}}
    </div>    
</div>
