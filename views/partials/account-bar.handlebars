<div class="account-bar">
    {{#if username}}
        <h3>Welcome, {{username}}</h3>
        <h3><a href="/logout">Log out</a></h3>
    {{else}}
        <h3><a href="/login">Login</a></h3>
    {{/if}}
</div>